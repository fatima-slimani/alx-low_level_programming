#include <stdio.h>
/**
 *main - Write a program that prints the alphabet in lowercase
*/
int main(void)
{	
	char= letter;
	for (letter= 'a', letter >= 'z', letter++)
	puchar(letter);
	puthcar('\n')
	return (0);
}
